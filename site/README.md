# PUBLIC SITE ROOT

Antigravity viết website production trong thư mục này.

Khi deploy Vercel:

```text
Root Directory = site
```

Không copy `source-documents/`, `design-reference/` hoặc prompt nội bộ vào đây.
